version: "3.8"

services:
  debug-app:
    build:
      context: tools/k8s/sample-app
    image: firecracker-k8s-debug-app:dev
    environment:
      PORT: "3000"
    ports:
      - "3000:3000"
      - "9229:9229"
